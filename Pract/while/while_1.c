#include<stdio.h>

int main(void)
{
    char y, z;
    int i;

    i = 0;
    while(i < 10);
    {
        scanf("%c", &y);
        if(y == "cat") printf("%c\n", z);
        i++;
    }

    return 0;
}